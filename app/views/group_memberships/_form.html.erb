<%= simple_form_for [@group, @group_membership] do |f| %>
  <%= f.error_notification%>
  <!-- po.input :product_id, collection: Product.all -->
  <div style="margin: 20px 0">
    <%= f.select :user_id, options_from_collection_for_select(User.all, :id, :email) %>
  </div>
  <%= f.button :submit, class: "btn btn-brand-with-bg" %>
<% end %>


<%#= f.input :location,:collection => @locations,:label_method => :location_name,:value_method => :id,:label => "Location" ,:include_blank => false %>


